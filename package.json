{
  "name": "builder-bricks",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "concurrently -n web,api -c cyan,magenta \"npm run dev --prefix frontend\" \"npm run start --prefix backend\"",
    "dev:web": "npm run dev --prefix frontend",
    "dev:api": "npm run start --prefix backend",
    "build": "npm run build --prefix frontend",
    "start": "concurrently -n web,api -c cyan,magenta \"npm run start --prefix frontend\" \"npm run start --prefix backend\"",
    "lint": "npm run lint --prefix frontend",
    "install:all": "npm install && npm install --prefix frontend && npm install --prefix backend"
  },
  "devDependencies": {
    "concurrently": "^10.0.4"
  }
}
